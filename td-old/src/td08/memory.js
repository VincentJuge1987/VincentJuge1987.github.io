class MemoryGame {
  constructor(images, blank) {
    // TODO
  }
  build = function(div) {
  // TODO
  }
}

const shuffleCards = function(length) {
  let cards = [];
  for(var i = 0; i < length; i++) {
    cards.push(i);
    cards.push(i);
  }
  return cards;
};
